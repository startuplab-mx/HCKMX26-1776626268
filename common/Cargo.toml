[package]
name = "common"
version = "0.1.0"
edition = "2021"
description = "Tipos y constantes compartidos entre app, server y dashboard"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
